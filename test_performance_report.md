# Subtitle QC Test Performance Report

Here is the progress of the test pass rate across iterations.

```mermaid
xychart-beta
    title "Pass Rate over Attempts"
    x-axis "Attempt" [1, 2]
    y-axis "Pass Rate (%)" 0 --> 100
    line [0, 100]
```
